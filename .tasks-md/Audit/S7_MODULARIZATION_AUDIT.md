# Sprint 7 Modularization Audit

| File | LOC | Top-level units (count, kinds) | Distinct responsibility clusters | Split candidates (proposed module names) | Coverage notes | Risk tier | Reason |
|---|---|---|---|---|---|---|---|
| `agent.py` | 9,899 | 1 class (`AgentLoop`), ~236 methods | run lifecycle dispatch, tool execution (139 `_tool_*` methods), replay engine (replay_one/replay_all + ~15 helpers), skill loading, plan/correction/assertion building, recording/codegen, locator + element resolution | `agent_tool_executor.py` – all `_tool_*` dispatch methods; `agent_replay_engine.py` – replay_one/replay_all + precondition helpers; `agent_recording_codegen.py` – step record / code update / locator-to-playwright; `agent_plan_builder.py` – plan-ready payloads, correction child building; `agent_skill_loader.py` – skill loading/prompt assembly | `tests/test_assertion_flow.py`, `tests/test_replay_one.py`, `tests/test_replay_all.py`, `tests/test_deterministic_fast_path.py`, `tests/test_plan_correction.py`, `tests/test_skill_loading.py` — broad but fragmented; inner seams NEED_CHARACTERIZATION per cluster | DEFER_S8_S9 | Entire run-loop state is shared mutable `self`; safe extraction of any cluster requires seam-level characterization tests first to avoid silent behavior regression |
| `frontend/src/main.jsx` | 3,176 | 74 functions, 2 React components (`AutoWorkbenchRuntime`, `useFrontendEventStore`) | normalize/coerce helpers, recorded-step model (build/merge/sort), trace entry model, element-info normalization, WebSocket transport + event reducer (`useAutoWorkbenchTransport`), mount/unmount API | `main-normalizers.js` – all `normalize*`/`coerce*` pure helpers; `main-step-model.js` – recorded/pending step build+merge+sort; `main-trace-model.js` – trace entry normalizers + merge; `main-transport.jsx` – `useAutoWorkbenchTransport` hook + WS reducer | `tests/test_frontend_event_store.py`, `tests/test_frontend_event_store_handlers.py`, `tests/test_frontend_live_state.py` — cover transport; normalizer layer partially covered by `tests/test_frontend_command_dispatcher.py` | BEFORE_S8 | Pure helper functions (≈1,200 LOC) have clean seams and no shared mutable state; extraction low-risk but needs import-path update across consumers |
| `runtime/llm_runtime_controller.py` | 2,058 | 9 module-level functions, 3 classes (`PurposeRegistry`, `ControllerTelemetry`, `LLMRuntimeController`), ~35 methods on LLMRuntimeController | purpose-policy registry, model-client selection + provider resolution, prompt-pack assembly, tool filtering, response validation + schema retry, telemetry emission, `call` / `call_with_raw_response` entry points | `llm_purpose_policy.py` – `PurposeRegistry` + all `_*_policy` builders (≈260 LOC); `llm_prompt_pack.py` – `_apply_prompt_pack` + `_prepare_messages` (≈150 LOC); `llm_response_validator.py` – `_validate_response` + schema-retry logic | `tests/test_llm_runtime_controller_contract.py`, `tests/test_llm_purpose_policy_*.py` (6 files), `tests/test_llm_policy_gateway.py` | BEFORE_S8 | `PurposeRegistry` and policy-builder block have no runtime side-effects and are independently testable; safe to extract with existing test coverage |
| `runtime/event_contracts.py` | 1,452 | 39 module-level functions, 0 classes | backend event envelope builders (step lifecycle), frontend command normalizers, plan-diff event builders, locator/recovery/error event builders, token report builder | `event_contracts_step.py` – step-lifecycle payloads (validating/executing/failed/skipped); `event_contracts_plan.py` – plan-diff proposed/validated/applied + plan-ready; `event_contracts_analysis.py` – page analysis + recommendation + capability-gap events; `event_contracts_command.py` – `normalize_frontend_command` + command normalizers | `tests/test_event_contract.py`, `tests/test_event_contracts.py`, `tests/test_event_sequence_contract.py` — good coverage | NOW | All functions are pure builders with no shared state; split is mechanical; existing contract tests act as characterization tests and will catch any import-path regressions immediately |
| `runtime/dom_locator_contract.py` | 1,317 | 28 module-level functions, 2 classes (`_Frame`, `_SnapshotParser`) | HTML snapshot parser, candidate ranking, locator validation, scope filtering, assertion target classification, dynamic UI classification, locator escalation + update command builders, fixture requirements builder | `dom_snapshot_parser.py` – `_SnapshotParser` + `build_page_snapshot`; `dom_candidate_ranker.py` – `rank_locator_candidates` + `_rank_score` helpers; `dom_locator_commands.py` – escalation + update command builders | `tests/test_dom_locator_contracts.py`, `tests/test_dom_locator_advanced_contracts.py`, `tests/test_dom_heavy_fixtures.py` | BEFORE_S8 | Parser + ranker are self-contained; commands block mixes contract shapes; needs minor import shuffle before S8 locator hardening |
| `frontend/src/v4/llm-cards.jsx` | 1,105 | 21 exports/functions, 15 exported card components + 2 hooks/utilities | card components (clarification, recommendation, plan-diff, plan-ready, permission, execution, locator ambiguity, recovery, completed, offline, schema-error), composer + LlmThread orchestrator, tiny format helpers | `llm-cards-planning.jsx` – `CardClarification`, `CardRecommendation`, `CardPlanDiff`, `CardPlanReady`; `llm-cards-execution.jsx` – `CardExecution`, `CardPermission`, `CardLocatorAmbiguity`, `CardRecovery`, `CardCompleted`, `CardOffline`, `CardSchemaError`; `llm-thread.jsx` – `Composer`, `LlmThread` | `tests/test_frontend_llm_cards.py`, `tests/test_frontend_llm_mode_complete.py` | DEFER_S8_S9 | Card props are stable but `LlmThread` orchestrates all cards with shared state; splitting cards from thread risks prop-drilling or context churn mid-sprint |
| `frontend/src/v4/secondary-tabs.jsx` | 1,044 | 20 functions, 4 exported tab components (`StepsTab`, `RecordedTab`, `CodeTab`, `TraceTab`) + internal sub-components | step editor + sub-components (kind chip, precondition strip, blocked strip, locator chip, child list), recorded step rendering, code tab + diagnostics, trace tab | `tab-steps.jsx` – `StepsTab` + all step sub-components; `tab-recorded.jsx` – `RecordedTab` + recorded-status helpers; `tab-code.jsx` – `CodeTab`; `tab-trace.jsx` – `TraceTab` | `frontend/tests-dom/secondary-tabs.test.jsx` (906 LOC) — strong characterization test | NOW | Tab components are already exported independently with no cross-tab shared state; `secondary-tabs.test.jsx` covers all four exports; split is low-risk and improves future per-tab iteration |
| `browser.py` | 1,540 | 13 module-level functions, 0 classes | asset reading + injection script builders, browser launch + CDP config, panel injection (large monolith at line 239–803), picker arm/binding/overlay install | `browser_launch.py` – `launch_browser`, `get_page`, port/URL readers; `browser_injection.py` – `inject_panel` + injection script builders; `browser_picker.py` – `arm_picker`, `_ensure_picker_binding`, `_install_picker_overlay` | `tests/test_browser_injection.py` — covers injection; picker + launch untested → NEEDS_CHARACTERIZATION | BEFORE_S8 | `inject_panel` is 564-line monolith (lines 239–803); picker is independent; both have clear seams but picker is untested — needs characterization before extract |
| `server.py` | 655 | 1 class, 13 module-level functions | WebSocket session lifecycle, run-session attach/expire/grace, command-state snapshot builder, legacy command normalization, replay JSON sender, FastAPI route handlers | `server_session.py` – `WebSocketRunSession` + attach/expire/grace helpers; `server_routes.py` – FastAPI route handlers (`lifespan`, `api_log`, `ws_endpoint`) | `tests/test_ws_reconnect_grace.py`, `tests/test_save_snapshot_ws.py`, `tests/test_websocket_command_event_integration.py` | DEFER_S8_S9 | Below 800-LOC flag threshold; session + route coupling is tight via shared `_active_run_session` global; splitting adds indirection without clear maintenance win yet |
| `frontend/aw-ide-panel.jsx` | 473 | 14 functions/components, 6 exported components | state-to-phase mapping, dispatcher factory, `IDEPanel` orchestrator connecting chrome + secondary-tabs, payload adapters for ambiguity/recovery/rejection | No natural split — all 473 lines are panel-orchestration glue; extracting payload adapters is possible but marginal | `tests/test_frontend_steps_manual_cards.py`, `frontend/tests-dom/panel-integration.test.jsx` | DEFER_S8_S9 | Under 800 LOC; single coherent orchestration responsibility; split candidates are thin adapters only |
| `frontend/src/v4/chrome.jsx` | 317 | 7 functions/components, 6 exported chrome components | header, tab strip, now-strip, footer, agents popover, collapsed rail | No split warranted — each component is already a focused atom | `tests/test_frontend_structure.py` | DEFER_S8_S9 | Under 800 LOC; components are already granular |
| `runtime/llm_runtime_controller.py` *(purpose-policy block only — sub-entry)* | — | — | — | Already covered in the main row above | — | — | — |
| **OVERALL VERDICT** | **-** | **Files >800 LOC: 8 (agent.py, main.jsx, llm_runtime_controller.py, event_contracts.py, dom_locator_contract.py, llm-cards.jsx, secondary-tabs.jsx, browser.py). Risk tiers: NOW=2 (event_contracts.py, secondary-tabs.jsx), BEFORE_S8=4 (main.jsx, llm_runtime_controller.py, dom_locator_contract.py, browser.py), DEFER_S8_S9=4 (agent.py, llm-cards.jsx, server.py, aw-ide-panel.jsx/chrome.jsx).** | **Sprint 7 action: AUDIT-ONLY for all files except two optional safe extractions — (1) `event_contracts.py` split into 4 sub-modules (pure builders, full test coverage, zero runtime risk) and (2) `secondary-tabs.jsx` split into 4 tab files (strong DOM test coverage, no shared state). All other files require characterization test prep before S8 extraction.** | — | — | — | — |
