# UI Defects — Tab-by-Tab Shadow DOM Audit

Tracking visible defects in the v4 panel after the design port. Each defect
includes the screenshot path used as evidence, the root cause, and the fix
status.

Audit method:
- `bash scripts/launch.sh` with `AUTOWORKBENCH_REMOTE_DEBUGGING_PORT=9222`.
- `python tools/audit_walk.py initial walk_tabs` produces a screenshot per tab
  under `/tmp/aw-audit/tab_*.png`.
- Verify against design source `frontend_new_design_prototype/yui/project/*`.

## Fixed

| ID | Tab | Symptom | Root cause | Fix |
|----|-----|---------|------------|-----|
| D-001 | All | Footer reads "PLANNING / LAST: TIMELINE" on fresh load | `useState` defaults for `runState` + `interactionMode` set to `"planning"` instead of `"idle"`; backend `AUTOWORKBENCH_DEFAULT_CONFIG` also seeded `state: "planning"` | `frontend/src/main.jsx` defaults flipped to `"idle"`; `toPanelState` default branch returns `"idle"`; `browser.py` bootstrap config now `state: "idle"` |
| D-002 | LLM | Now strip shows "Backend is drafting a plan." at idle | Side effect of D-001 (state=planning → meta.kind=run → strip rendered) | Resolved by D-001 |
| D-003 | LLM | `DraftPendingPanel` ("Run Pending Steps" card) renders before any user activity | Auto-created empty pending step satisfied the old condition `pendingSteps.length > 0` | `LlmThread` now checks `pendingSteps.some(s => s.intent.trim() || s.element_info)` so empty drafts stay hidden |
| D-004 | All | `harness.py` selectors targeted legacy `#aw-root` shadow id that does not exist in v4 | v4 host id is `#autoworkbench-root` with `#aw-shadow-mount` inside the shadow root | `find_autoworkbench_panel`/`find_autoworkbench_state_locator`/`wait_for_autoworkbench_ready` use the v4 ids |
| D-005 | Backend | `[FRONT]` log lines not visible in `/tmp/aw-launch.log` | No frontend → backend log ingest path | Added `runtime/log.py` + `POST /api/log` + `frontend/src/log.js` with ring buffer, console, and backend POST |

## Open

| ID | Tab | Symptom | Root cause | Plan |
|----|-----|---------|------------|------|
| D-101 | Steps | Design's per-row state badges (strong/weak locator, section step, missing-data block, wrong-page warning, child-op count) are not rendered | `frontend/src/v4/secondary-tabs.jsx::StepsTab` is the pre-design layout; backend doesn't yet emit the underlying signals | **Deep workflow portion fixed in Pass 4a** (intent input, outcome chips, Attach Element → `arm_picker`, target summary, ready badge, typed `run_steps` dispatch, Run-selected, Run-blocked, malformed-step safety — covered by jsdom in `panel-integration.test.jsx::Steps tab deep workflow*` and `secondary-tabs.test.jsx::StepsTab*`). **Locator-strength seam fixed in Pass 4b-1**: `runtime/locator_intelligence.py::classify_locator_strength_from_selector` + `annotate_plan_steps_with_locator_kind` annotate `plan_ready` step payloads (`agent.py:9141`); `browser.py::on_picked` classifies element_info; frontend `StepLocatorChip` renders `data-testid="step-locator-${stepId}"` only when backend provides `locator_kind`. Backend tests (`tests/test_locator_strength_annotator.py`, 22 tests) + jsdom tests (`secondary-tabs.test.jsx`, 5 chip tests). Improve-locator / View-candidates inline actions deferred to Pass 4b-1.1 (no backend command exists yet). **Step-kind seam fixed in Pass 4b-2**: `runtime/step_metadata.py::classify_step_kind` + `annotate_plan_steps_with_kind` derive `step.step_kind ∈ {atomic, loop, section, unknown}` from intent regex (`each`/`every`/`for each`/`all rows-cards-...`) and child-op count (≥2 children → section). Wired into `agent.py::_send_plan_ready_after_confirmation` alongside locator annotator. Frontend `StepKindChip` renders `data-testid="step-kind-${stepId}"` with `data-kind` (clamped) + `data-raw-kind` (preserved). Backend tests (`tests/test_step_metadata.py`, 22 tests) + jsdom tests (`secondary-tabs.test.jsx`, 6 chip tests). **Section children seam fixed in Pass 4b-3**: `runtime/step_metadata.py::normalize_plan_steps_children` runs before kind classification on plan_ready, guaranteeing `step.children` is a list of dicts with stable `child_id` (synthesized as `op_${i+1}` when backend omits it); malformed entries dropped; non-list children clamped to `[]`. Frontend `StepChildrenList` renders `step-children-${stepId}` only when at least one valid dict child exists, with per-row `step-child-${stepId}-${childId}`, `step-child-label-${...}`, `step-child-status-${...}` testids; status row only renders when backend provides it. Backend tests (`tests/test_step_metadata.py`, +8 tests) + jsdom tests (`secondary-tabs.test.jsx`, +8 tests). **Blocked-step seam fixed in Pass 4b-4**: `runtime/step_metadata.py::normalize_plan_steps_blocked` validates `step.blocked.reason` against {missing_data, wrong_page, locator_unstable, permission_required, unknown} (with `weak_locator → locator_unstable` alias), cleans `refs`, drops non-dict blocked values entirely. Frontend `StepBlockedStrip` renders with `data-reason` clamped + `data-raw-reason` preserved. Status badge reads "blocked" instead of "ready". **Precondition seam fixed in Pass 4b-5**: `normalize_plan_steps_precondition` validates `step.precondition.status` against {passed, failed, unknown}, preserves `expected_url` / `current_url` / `message` as strings. Frontend `StepPreconditionStrip` renders only when `status === "failed"`; never infers wrong-page state from text. Disabled `Change precondition` action deferred to Pass 4b-5.1. **Child-count seam fixed in Pass 4b-6**: `normalize_plan_steps_child_count` preserves explicit non-negative int; otherwise derives from `len(children)` when children is a list; absent if neither source available; rejects bool/negative/non-int. Frontend `StepChildCountBadge` renders `step-child-count-${stepId}` with `data-count` and pluralization. **Pass 4b-4.1 DONE (blocked-action resolve commands)**: `StepBlockedStrip` button enabled; `_blockedActionCommand(reason, stepId)` maps `missing_data→correction`, `permission_required→permission_decision`, `locator_unstable→improve_locator`, `wrong_page→navigate_to_expected`, `unknown→skip_step`. Backend: `change_precondition` + `navigate_to_expected` added to `SUPPORTED_FRONTEND_COMMAND_TYPES`; `server.py` handlers emit `step_precondition_updated` / `navigate_to_expected_acknowledged` respectively; routed through correction pipeline (no `code_update`/`step_recorded`). `aw-ide-panel.jsx` + `main.jsx` wired: `onResolveBlocked`, `onChangePrecondition`, `onNavigateToExpected` dispatchers threaded to `StepsTab`. Tests: `tests/test_d101_state_commands.py` 18 backend contract tests GREEN; `secondary-tabs.test.jsx` +12 D-101 jsdom tests (5 reason-routing tests, 2 precondition tests, 2 navigate-guard tests, 1 disabled-title, 2 safety tests) GREEN. pytest: 2611 passed. jsdom: 136 passed (4 files). **Pass 4b-5.1 DONE (change-precondition command)**: `StepPreconditionStrip` change-precondition button enabled; new `step-navigate-expected-${stepId}` button renders only when `status === "failed"` AND `expected_url` is non-empty string. **Pass 4b-1.1 DONE (improve_locator / view_candidates / change_locator_scope commands)**: `runtime/event_contracts.py` adds `improve_locator`, `view_candidates`, `change_locator_scope` to `SUPPORTED_FRONTEND_COMMAND_TYPES`; `server.py` handlers validate payload (step_id required, scope required for change_locator_scope) and reject malformed via typed rejection. `StepLocatorChip` renders `step-improve-locator-${stepId}` and `step-view-candidates-${stepId}` inline buttons when `locator_kind ∈ {warn, med, weak, unknown}` and step has id. `aw-ide-panel.jsx` + `main.jsx` thread `onImproveLocator` + `onViewCandidates` dispatchers via runtime. `step-change-locator-scope-${stepId}` inline is DISABLE_WITH_REASON (no per-step scope-text UI for Sprint 7); card-level `locator-change-scope` in CardLocatorAmbiguity stays wired. Tests: `tests/test_d101_locator_commands.py` 27 backend contract tests GREEN; `secondary-tabs.test.jsx` +12 D-101 locator jsdom tests GREEN. **All D-101 command-only sub-passes (4b-1.1 / 4b-4.1 / 4b-5.1) DONE.** All visual seams from the design's per-row state badges are now backed by validated backend metadata; frontend never invents state — see Pass 1B rule against static-mock-as-live-state. |
| D-102 | Recorded | Tab body renders the legacy minimal list ("No recorded steps yet. They appear here after `step_recorded`…") instead of the design's per-row evidence card (locator used, validation count, observed vs expected, screenshot link, replay status) | `RecordedTab` not redesigned | **Fixed in Pass 5**: `RecordedTab` reshaped to surface backend evidence honestly — `recorded-row-${id}`, `recorded-status-${id}` (`data-status` ∈ {recorded, repaired, skipped, failed, unresolved, unknown}), `recorded-locator-${id}` with `data-locator-kind`, `recorded-expected-${id}` / `recorded-observed-${id}` gated on payload, `recorded-child-list-${id}` (`data-count`) + per-row `recorded-child-${stepId}-${childId}` (`data-op-type` / `data-op-status`), and `recorded-artifact-${stepId}-${artifactId}` for backend-emitted artifact links. Replay button is disabled with explanatory `title` when the step has no backend id or no `onReplayOne` handler — no fake replay path. Malformed step entries are filtered up front so the frontend never invents an evidence row. Tests: `secondary-tabs.test.jsx` (+13 D-102 tests), all pre-existing recorded/source-pattern tests preserved. |
| D-103 | Code | Code tab shows "Awaiting code_update event. No code rendered yet." with minimal controls; design has copy/save/file-path/diagnostics panel | `CodeTab` not redesigned | **Fixed — D-103 CLOSED**: backend `export_code` typed command added to `runtime/event_contracts.py::SUPPORTED_FRONTEND_COMMAND_TYPES`; `server.py` handler writes spec into workspace and emits `export_code_result {ok, path|error}`. **Security**: handler resolves candidate path to realpath and rejects writes outside workspace (relative `..` escape, absolute outside, symlink escape) with `path must be inside the workspace`. Frontend `IDECodePreview` (in `secondary-tabs.jsx`) gains `code-save-result` testid with `data-status`/`data-path`/`data-error`; `main.jsx` `handleExportCode` dispatches typed command and `handleCopyCodeToClipboard` keeps clipboard local; reducer adds `export_code_result` case that clears `codeSaveResult` on next `code_update`. Tests: `tests/test_export_code_handler.py` 15 backend contract tests including 4 path-traversal cases (relative escape, absolute escape, symlink escape, inside-workspace allowed); `frontend/tests-dom/secondary-tabs.test.jsx` 13 D-103 jsdom tests for empty state, render, copy, save, diagnostics, save-result chip, malformed payload safety. Architecture invariant: no frontend code generation; frontend renders only backend `code_update` payload. — **CLOSED** |
| D-104 | Trace | Trace tab shows raw event list ("ready", "session_state"). Design wants filter chips by category + structured rows + failure detail panel | `TraceTab` not redesigned | **Fixed — Pass D-104**: TraceTab now has: (1) gap chip (`trace-filter-gap`) with exact-match predicate `type === "capability_gap_recorded"` — 7 chips total (all/llm/step/permission/error/code/gap); (2) text filter checks `summary` field; (3) click-to-expand rows for step_failed / llm / gap types; (4) failure-detail panel (`trace-failure-detail-${i}`) renders step_id, error, status, operation_id from payload — no fabricated fields; (5) LLM telemetry section with honest `trace-llm-unavailable-${i}` when fields absent; (6) artifact list (`trace-artifact-list-${i}`) with `data-artifact-href` only when path present; (7) redaction chip (`trace-redaction-chip-${i}`) conditional on `r.redactionStatus`; (8) capability-gap card (`trace-gap-card-${i}`) with gap_id, needed_capability, path. Tests: `secondary-tabs.test.jsx` +13 D-104 tests (all GREEN). `V4_TESTID_CONTRACT.md §9` updated with all new testids. — **CLOSED** |
| D-105 | Header | "LLM/Manual" mode toggle renders but flipping does nothing visible | `setMode` only updates panel local state, doesn't show `ManualBuilder` (design's manual step composer) | Either wire `ManualBuilder` (design) into Steps tab, or remove the toggle until backend supports manual-mode commands |
| D-106 | Header | Agents popover lists default DEFAULT_AGENTS mock; backend doesn't emit `agent_settings` so toggles dispatch no command | Backend lacks agent registry events | Document as orphan; render real list once backend grows the contract |
| D-107 | Composer | Pick element / camera buttons in Composer have no handler | Pickelement wiring lives only on the Steps tab | **Fixed — D-107 CLOSED**: `handleComposerPick` added to `useAutoWorkbenchTransport` in `main.jsx`; auto-creates blank pending step, dispatches typed `arm_picker` with its `step_id`, and sets `activePickerStepId`. `aw-ide-panel.jsx` threads `handleComposerPick` (not the Steps-tab `handleAttachElement`) into Composer `onPickElement`. Button title updated to "Pick a page element to attach to your task". Camera/screenshot button absent from DOM (HIDE_AS_NON_P0; deferred to Sprint 8 pending backend screenshot command). `disabled` prop gates button when `status === "offline"` or `runtime.composerDisabled`. jsdom tests: `llm-cards.test.jsx` "v4 Composer pick (D-107)" describe block — 6 tests, all GREEN. — **CLOSED** |
| D-108 | LlmThread | `CardRecommendation`, `CardPlanDiff`, `CardSchemaError`, `CardOffline` cards exist with mock content; some still render mock copy because LlmThread checks state-only gates without payload presence | Orphan cards before backend event contracts exist | Already gated `CardRecommendation` and `CardPlanDiff` on payload; do same for any remaining mock-only cards |

## How to reproduce

```
# fresh launch with debug port + headed Chromium
lsof -ti:8765 | xargs kill -9 2>/dev/null
rm -rf .pw-user-data
AUTOWORKBENCH_REMOTE_DEBUGGING_PORT=9222 bash scripts/launch.sh > /tmp/aw-launch.log 2>&1 &
sleep 12
python tools/audit_walk.py initial walk_tabs
# Screenshots: /tmp/aw-audit/tab_*.png
# Backend log: /tmp/aw-launch.log (includes frontend ingest via /api/log)
```
